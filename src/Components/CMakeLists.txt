# Add all components here using ADD_COMPONENT(<COMPONENT_DIRECTORY>)
ADD_COMPONENT(FindEdges)

ADD_COMPONENT(DrawEdges)

ADD_COMPONENT(DrawSomeEdges)

ADD_COMPONENT(DrawLines)

ADD_COMPONENT(DrawCorners)

ADD_COMPONENT(DrawHarris)
